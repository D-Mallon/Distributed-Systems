package service.message;

public class RegisterMessage {
    // This class can be left empty as we're just using it as a signal for
    // registration. Note - revert back if this any issues come from this.
}
